export const baseUrl = "https://frontend-take-home-service.fetch.com";

export const authUrl = `${baseUrl}/auth/login`;
export const breedsUrl = `${baseUrl}/dogs/breeds`;
export const dogsUrl = `${baseUrl}/dogs`;
export const matchesUrl = `${baseUrl}/dogs/match`;
export const searchPath = `/search`;
